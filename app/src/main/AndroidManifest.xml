<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.portal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".change_password"></activity>
        <activity android:name=".profile_update" />
        <activity android:name=".Companyprofile" />
        <activity android:name=".comp_applications" />
        <activity android:name=".stud_applications" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".student_job_activity" />
        <activity android:name=".student_intern_activity" />
        <activity android:name=".descriptionActivity" />
        <activity android:name=".add_intern_or_job_activity" />
        <activity android:name=".studentLandingPage" />
        <activity android:name=".companyLandingPage" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".LogInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>